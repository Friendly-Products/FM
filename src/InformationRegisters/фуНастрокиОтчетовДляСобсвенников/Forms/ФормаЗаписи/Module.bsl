

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	ПериодВЗапросе.ДатаНачала	 = Запись.ДатаНачала;
	ПериодВЗапросе.ДатаОкончания = Запись.ДатаОкончания;

КонецПроцедуры


&НаСервере
Процедура ПериодВЗапросеПриИзмененииНаСервере()
	
	Запись.ДатаНачала = ПериодВЗапросе.ДатаНачала;
	Запись.ДатаОкончания = ПериодВЗапросе.ДатаОкончания;

КонецПроцедуры


&НаКлиенте
Процедура ПериодВЗапросеПриИзменении(Элемент)
	ПериодВЗапросеПриИзмененииНаСервере();
КонецПроцедуры

